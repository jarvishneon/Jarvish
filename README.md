# Jarvish
# One-Link Exploit & Keylogger Demo (For Educational/Pentesting Use Only)

## Features
- Universal keylogger (any login/input on the page)
- Socket.io client/server connection
- Device info, location, permissions
- Anti-detection patches
- Service worker for future background features

## Usage
1. Clone repo, run `npm install`
2. Start server: `node server.js`
3. Open `http://localhost:3000` in browser/device
4. Allow permissions, fill any form/input — data will be logged in `keylog.txt`

**Warning:**  
Use only for ethical hacking, learning, or authorized pentesting.  
Never use on real users/websites without permission!
